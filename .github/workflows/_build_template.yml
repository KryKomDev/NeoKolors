name: Build Template

on:
  workflow_call:
    inputs:
      project_name:
        required: true
        type: string
      project_path:
        required: true
        type: string
      test_project_path:
        required: true
        type: string

jobs:
  build:
    strategy:
      matrix:
        configuration: [Debug, Release]

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        cache: true

    - name: Restore Projects
      run: dotnet restore ${{ inputs.project_path }} --use-lock-file

    - name: Build Projects
      run: dotnet build ${{ inputs.project_path }} --configuration ${{ matrix.configuration }} --no-restore

    - name: Execute unit tests
      run: dotnet test ${{ inputs.test_project_path }} --configuration ${{ matrix.configuration }} --no-build
